<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-100">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Job Board - Job Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-full flex flex-col">
    <header class="bg-white shadow">
        <nav class="container mx-auto px-4 py-6">
            <a href="/" class="text-xl font-bold text-gray-800">Job Description</a>
        </nav>
    </header>

    <main class="flex-grow container mx-auto px-4 py-8">
        <div class="bg-white shadow overflow-hidden sm:rounded-lg">
            <div class="px-4 py-5 sm:px-6">
                <h2 class="text-2xl font-bold text-gray-900"><%= job.company %></h2>
                <h3 class="mt-1 text-xl text-gray-600"><%= job.title %></h3>
            </div>
            <div class="border-t border-gray-200 px-4 py-5 sm:px-6">
                <div class="grid grid-cols-1 gap-x-4 gap-y-8 sm:grid-cols-2">
                    <div class="sm:col-span-2">
                        <div class="text-sm font-medium text-gray-500">Job Description</div>
                        <div class="mt-1 text-sm text-gray-900 whitespace-pre-wrap"><%- job.description %></div>
                    </div>
                    <div class="sm:col-span-1">
                        <div class="text-sm font-medium text-gray-500">Location</div>
                        <div class="mt-1 text-sm text-gray-900"><%= job.location || 'Not specified' %></div>
                    </div>
                    <div class="sm:col-span-1">
                        <div class="text-sm font-medium text-gray-500">Job Type</div>
                        <div class="mt-1 text-sm text-gray-900">Full-time</div>
                    </div>
                    <div class="sm:col-span-2">
                        <a href="<%= job.apply_url %>" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                            Apply Now
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer class="bg-gray-800 text-white py-4">
        <div class="container mx-auto px-4 text-center">
            <p class="text-sm">
                NoCopyright &copy; <script>document.write(new Date().getFullYear());</script> | Made by some kids at GTBIT
            </p>
        </div>
    </footer>

    <script>
        // Any necessary JavaScript can be included here
    </script>
</body>
</html>